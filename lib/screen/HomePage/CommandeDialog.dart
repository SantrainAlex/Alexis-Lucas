import 'package:flutter/material.dart';

class CommandeDialog extends StatefulWidget {
  const CommandeDialog({Key? key}) : super(key: key);

  @override
  State<CommandeDialog> createState() => _CommandeDialogState();
}

class _CommandeDialogState extends State<CommandeDialog> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
